interface:
  display_name: "Code Context"
  short_description: "Query managed repositories for precise code context."
  default_prompt: "Use $code-context via its v1 HTTP API to plan and batch parallel code searches, filter the relevant evidence, and answer from the selected code."

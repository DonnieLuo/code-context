interface:
  display_name: "Code Context"
  short_description: "Get code context from managed repositories."
  default_prompt: "Use $code-context via its v2 HTTP API. Start with build_context, then use impact or precise Java tools only when needed."
